package main

oneLiner -> fmt.Println("a")

testBody ->
  fmt.Println("a")

testArg (str string) ->
  fmt.Println(str)

arrArg (str []string) ->
  fmt.Println(str)

doubleArrArg (str [][]string) ->
  fmt.Println(str)

doubleArrPointerArg (str **[]*[]*string) ->
  fmt.Println(str)

testRet (str string): string ->
  return str

testRet2 (str string, str2 string): string, string ->
  return str, str2

testAssign ->
  a := foo()
  a, b := foo()
  a, b, c := foo()
