!walker

import
	"encoding/gob"
	"github.com/champii/og/lib/common"

struct GobRegister
	AstWalker
	*Each(n common.INode) -> gob.Register(n)

RunGobRegister(tree common.INode) ->
	p := GobRegister{}
	gob.Register(tree)
	p.type_ = &p
	p.Walk(tree)
